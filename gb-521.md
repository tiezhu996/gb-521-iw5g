# gb-521 矿井通风网络联锁推演

请生成英文短名为 `mine-ventilation-network-simulator` 的 Go 全栈 Web 项目。系统面向矿井通风技术人员，在离线模型中维护通风网络、比较风机方案、推演风量分配并复核联锁风险。它只提供决策支持和仿真结果，绝不能连接 PLC、风机、风门或任何现场控制系统，不能把计算结果表述为可直接执行的安全指令；所有方案必须保留人工复核和批准步骤。项目不包含工单、库存、采购、订单、财务或大众社区功能。

## 项目主要需求

### 1. 核心实体

以下 4 个实体都必须贯穿 PostgreSQL 表、Go model/dto/repository/service/handler/router、前端 types/api/store/page，严禁只做静态页面或只在单层出现：

- `VentilationNode`：字段至少包含 `id`、`code`、`node_type`、`elevation_m`、`required_airflow_m3s`、`pressure_pa`、`status`、`created_at`；表示进风口、回风口、工作面或网络交点，并关联多条巷道边。
- `AirwayEdge`：字段至少包含 `id`、`from_node_id`、`to_node_id`、`resistance_ns2m8`、`area_m2`、`max_velocity_ms`、`door_state`、`enabled`、`version`；两端必须引用不同节点，阻力和面积必须为正数。
- `FanScenario`：字段至少包含 `id`、`name`、`fan_curve_json`、`operating_mode`、`scenario_status`、`version`、`created_by`、`approved_by`；保存风机曲线、边界条件和审批版本。
- `SimulationRun`：字段至少包含 `id`、`scenario_id`、`run_status`、`iteration_count`、`residual`、`node_pressures_json`、`edge_flows_json`、`risk_flags_json`、`started_at`、`finished_at`；记录可重放的输入快照和结果，不得覆盖历史运行。

### 2. 核心页面

至少实现以下 5 个路由页面，每页真实调用 API，且至少消费 2 个实体：

- `/network` 通风网络编辑器：消费 `VentilationNode`、`AirwayEdge`，支持节点/边增改、连通性校验和只读拓扑图。
- `/scenarios` 方案版本页：消费 `FanScenario`、`AirwayEdge`，支持草稿、提交复核、批准和版本对比。
- `/simulations` 推演工作台：消费 `FanScenario`、`SimulationRun`，可发起离线计算、查看收敛过程和结果差异。
- `/interlocks` 联锁风险页：消费 `AirwayEdge`、`SimulationRun`，展示风速超限、反向流、断路和需求缺口，必须由人员确认。
- `/audit` 审计追踪页：消费 `FanScenario`、`SimulationRun`，按操作者、状态和时间筛选不可变审计事件。

### 3. 差异算法与状态流

- 实现可测试的有向网络校验：检测孤立节点、断路、非法自环、不可达工作面和边界条件缺失。
- 实现确定性的迭代风量平衡模拟器：依据巷道阻力关系计算节点压力和边风量，每轮记录最大残差，在阈值或迭代上限处结束；必须正确区分 `converged`、`not_converged`、`invalid_input`，不能用随机数伪造结果。
- 联锁规则至少覆盖风速上限、工作面最低风量、反向流和关键路径中断，并输出规则编号、证据值、阈值和等级。
- `FanScenario` 状态流固定为 `draft -> pending_review -> approved -> archived`，只有已批准方案可创建推演；驳回回到 `draft` 并保留原因，状态迁移使用事务和条件更新防止并发越级。

### 4. 横切关注点

- JWT 认证与 RBAC：角色至少有 `engineer`、`reviewer`、`admin`。权限必须联动用户/角色数据、认证与授权中间件、后端路由、前端路由守卫、按钮显隐和共享类型。
- 操作审计：实体增改、方案状态迁移、推演启动和风险确认均记录 request ID、操作者、对象、前后状态与时间；审计记录禁止通过普通 API 修改或删除。
- 全局错误处理：参数错误、冲突、无权限和算法不收敛使用统一错误码与 HTTP 状态，前端集中展示且不能吞错。
- 请求追踪与限流：结构化日志透传 request ID；登录和推演启动接口分别限流，限流实现不得依赖 Redis。

### 5. 共享枚举、组件、hooks 与中间件

- 枚举 `ScenarioStatus = draft | pending_review | approved | archived`，后端定义在 `backend/internal/constants/scenario.go`，前端定义在 `frontend/src/types/scenario.ts`。
- 枚举 `SimulationStatus = queued | running | converged | not_converged | invalid_input | failed`，后端定义在 `backend/internal/constants/simulation.go`，前端定义在 `frontend/src/types/simulation.ts`。
- README 必须列出上述枚举在数据库约束、后端 model/dto/service/handler 和前端 types/store/component/page 的全部出现位置。
- `components/common/` 至少包含 `StatusBadge`（方案与推演页共用）、`RiskEvidenceTable`（推演与联锁页共用）、`ConfirmActionDialog`（方案与联锁页共用）。
- `hooks/` 至少包含 `useAuth`、`useSimulationPolling`，不得把对应逻辑复制到页面。
- 后端至少实现 `RequestIDMiddleware`、`AccessLogMiddleware`、`AuthMiddleware`、`RBACMiddleware`、`RecoveryMiddleware`、`RateLimitMiddleware`，每个独立文件且有测试。

### 6. 技术与规模红线

- 前端：React 18 + TypeScript + Vite + Ant Design，独立 API 层、Zustand store、React Router；不得使用纯静态 mock 代替后端。
- 后端：Go 1.22 + Gin + GORM + PostgreSQL，使用 validator/v10、slog 或 zap、构造器注入、`%w` 错误包裹、事务和优雅停机。
- Go 功能代码目标 2500-4200 行、24-42 个功能 `.go` 文件且必须低于 5000 行；不计 `_test.go`、vendor、生成代码和前端代码。禁止用空壳文件、重复样板或超长单文件凑规模。
- API 统一使用 `/api/v1`，提供真实 `/healthz` 和 `/readyz`；响应格式、分页、时间格式和错误码统一。
- 禁止 TODO、placeholder、空函数、硬编码成功响应和绕过 repository 的 handler。多步写入必须事务化，关键唯一性由数据库约束保证。

### 7. 文件结构强制清单

根目录必须提供 `go.work` 并 `use ./backend`。至少遵循以下结构，严禁合并职责到单一文件：

```text
./
├── go.work
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/
│       ├── config/
│       ├── model/
│       ├── dto/
│       ├── repository/
│       ├── service/
│       ├── handler/
│       ├── router/
│       ├── middleware/
│       ├── constants/
│       └── util/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/
│       ├── api/
│       ├── stores/
│       ├── types/
│       ├── components/common/
│       ├── hooks/
│       ├── pages/
│       ├── router/
│       └── utils/
└── output/
    └── execution.md
```

`runtime_smoke.json` 只能是流水线可解析的启动 manifest，禁止写入构建、API 或 Browser 验收结果。必须使用以下字段和值；后端配置层在 `DB_DRIVER=sqlite` 时使用 GORM SQLite 和独立内存数据库，在 Compose 默认环境下仍使用 PostgreSQL：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20521",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb521-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20521/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- 英文 Compose 项目名固定为 `mine-ventilation-network-simulator`。前端端口 `18521`、后端端口 `19521`、PostgreSQL 宿主端口 `57521`；容器内端口分别为 `80`、`8080`、`5432`。
- 根目录必须有完整 `docker-compose.yml`、`.env`、`.env.example`、前后端 Dockerfile 和 Nginx 配置。Compose 顶层写 `name: mine-ventilation-network-simulator`，不得写 `version:`；容器名使用 `${COMPOSE_PROJECT_NAME:-mine-ventilation-network-simulator}` 前缀。
- PostgreSQL 使用命名卷和 healthcheck；后端以 `condition: service_healthy` 等待数据库，后端和前端均配置 healthcheck，前端按后端健康状态依赖。`docker compose ps` 中所有长驻服务最终必须为 healthy。前端只请求 `/api`，Nginx 转发至 `backend:8080` 并支持 SPA fallback。
- `.env` 与 `.env.example` 至少包含 `COMPOSE_PROJECT_NAME`、`FRONTEND_PORT=18521`、`BACKEND_PORT=19521`、`DB_PORT=57521`、数据库账号密码、JWT 密钥、CORS 白名单和日志级别。项目必须可在任意中文目录通过 `docker compose up -d --build` 启动。
- 中文 README 将 Docker 快速启动放在最前，明确 `cp .env.example .env`、访问地址、测试账号、功能、技术栈、目录、环境变量、API 清单、枚举位置、算法假设、安全边界、开发命令、故障排查和 License。

### 9. 强制真实验收

编码后必须真实执行并修复所有错误，禁止仅在文档中宣称通过：

1. 在项目根目录执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`，确认功能 Go 代码实测为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；再执行运行冒烟自检，验证上述 manifest 和真实服务入口。
2. 在 `frontend/` 执行依赖安装、类型检查、单元测试（如已配置）和生产构建，至少保证 `npm run build` 通过。
3. 执行 `docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`，确认数据库和后端 healthy、前端可访问；验证 `/healthz` 和 `/readyz`。
4. 至少 8 项真实接口冒烟：登录、节点创建、巷道创建、方案创建、提交复核、批准、启动推演、查询结果、风险确认和审计查询中任选不少于 8 项，覆盖成功、无权限和非法状态。方法、路径、状态码、关键断言和时间只能写入 `output/execution.md`，不得塞入 `runtime_smoke.json` 或伪造。
5. 只能使用 Codex 内置 Browser 验证登录、5 个主要页面、网络编辑、方案审批、推演结果和风险确认；禁止外部 Chrome。截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
6. 验收后执行 `docker compose down -v --remove-orphans`，再次执行 `docker compose ps` 并确认项目容器为空；不得清理其他项目容器。
7. 在本项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，把该 hash 写入 `output/execution.md`，再将 `output/execution.md` 与截图作为第二次报告提交。最终 `git status --short` 必须为空；不要在多个项目外层共用一个仓库。
